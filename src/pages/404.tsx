import { NextPage } from "next";
import React from "react";
import { Layout } from "~/components/Layout";

const NotFound: NextPage = () => {
  return <Layout>お探しのページは見つかりませんでした</Layout>;
};

export default NotFound;
